[package]
name = "ingest"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
cam = { version = "0.1.0", path = "../cam" }
pcb-core = { workspace = true }
serde_json = "1.0.150"
usvg = "0.47.0"

[dev-dependencies]
testkit = { path = "../testkit" }
proptest = "1.11.0"
image = { version = "0.25", default-features = false, features = ["png"] }
